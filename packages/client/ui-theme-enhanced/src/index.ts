/**
 * Theme Enhanced UI plugin, host half. The browser half ships the Theme
 * Enhanced settings section through exports["client"]; this entry exists so
 * the plugin appears in the host Loader tree like every sibling surface
 * plugin.
 *
 * @module @deepseek-ai/dsh-client-ui-theme-enhanced
 */

/** Host plugin body — no host-side behavior for this surface plugin. */
export function apply(): void {}
